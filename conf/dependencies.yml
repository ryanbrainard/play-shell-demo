# Application dependencies

require:
    - play
    - play -> crud
    - play-shell -> play-shell

repositories: 
    - vendored-modules: 
        type: local 
        artifact: ${application.path}/vendor/[module] 
        contains: 
            - play-shell
